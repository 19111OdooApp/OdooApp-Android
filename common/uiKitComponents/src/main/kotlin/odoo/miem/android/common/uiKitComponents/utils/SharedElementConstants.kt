package odoo.miem.android.common.uiKitComponents.utils

import com.mxalbert.sharedelements.ProgressThresholds

/**
 * [SharedElementConstants] - constants for SharedElement library
 * specifies some parameters for shared elements transitions
 */
object SharedElementConstants {
    const val transitionDurationMills = 800
    val progressThreshold = ProgressThresholds(0.1f, 0.4f)
}
